# Results scaffold — evidence gated and intentionally result free

> Status: paragraph and display slots were frozen before formal completion.
> Bracketed fields must be populated only from strict formal, confirmation,
> replication and descriptive-projection evidence. This file contains no
> assumed effect direction.

## One-sentence section argument

Across the complete registered synthetic design, we report whether
resource-matched hypergraph service contrasts reproduce across independent
formal and confirmation phases, followed by bounded parent-model, censoring,
activity and descriptive consistency analyses.

## 1. Complete execution and independent evidence

`[Insert phase completion: 240/240 formal and 240/240 confirmation blocks,
execution/analysis revisions, environment, total runtime and strict replay
fingerprints.]` Each phase contained 20 independent parent graphs in each of
the ER-GNM, BA and fixed-count SBM strata at every registered network size.
Thus, each phase-level contrast used `n=60` independent parents (`20+20+20`);
the seven held-out traffic traces per parent were nested measurements rather
than additional replicates. Phase-specific parent realizations were generated
from disjoint root-seed families, the traffic-seed families were disjoint, and
the two evidence chains were replayed separately. No cross-phase pooling was
performed. Multiplicity control applies separately to the 40 contrasts in each
phase and does not claim joint 95% coverage over the 80 cross-phase intervals.

## 2. Equal-family global contrasts

`[Insert the complete eight-row global registry from Table 1 and Fig. 2. For
each endpoint and size, report formal estimate [adjusted lower, upper], formal
interval direction; confirmation estimate [adjusted lower, upper], confirmation
interval direction; and the exact cross-phase replication state.]`

Lead with the cross-phase state rather than whichever phase has the larger
point estimate. Use exactly one of the following evidence-bound templates per
row:

- `independently_confirmed`: "The registered beneficial direction was supported
  in both phases ..."
- two harmful intervals: "Both phases placed the adjusted interval in the
  registered harmful direction ..."
- `formal_only` or `confirmation_only`: "Beneficial support was confined to the
  [formal/confirmation] phase and therefore did not meet independent
  confirmation ..."
- otherwise: "The two phase intervals were [directions]; this record was
  inconclusive or discordant and did not establish equivalence ..."

Do not compress several sizes into an unqualified endpoint-wide claim unless
all four rows have the same registered state and the sentence names that scope.

## 3. Named topology-family contrasts under global gates

`[Insert all 32 named-family rows from Fig. 3 and the complete source-data
table. Report the phase-specific global gate before interpreting each named
interval.]`

For each phase, a closed global gate makes the named result descriptive even
when its own adjusted interval lies beyond zero. A named family is labelled
independently confirmed only when its adjusted interval is beneficial and its
corresponding global gate is open in both phases. Do not rank families by point
estimate, count of favourable cells or visual intensity.

## 4. Parent-model strata

`[Insert ER-GNM, BA and fixed-count SBM parent summaries for every discussed
global or named row, including all 20 parent values per stratum in Source
Data.]` Describe compatible or differing directions as model-stratified
patterns. Do not call a compatible pattern robust, and do not infer an
interaction from one stratum crossing zero while another does not.

## 5. Censoring and event coverage

`[Insert every q=0.10 event-coverage cell by phase, size, parent model, source
family and traffic scope.]` Report the number of parents for which the source
and every registered binary arm matched to that source family met the
identification threshold. No
numerical `q=0.10` stopping-time quantile is estimated, regardless of whether a
cell passes the coverage gate.

## 6. Demand-aware activity sensitivity

`[Insert all changed and unchanged demand-aware activity cells for both primary
endpoints and both phases, including empty and singleton strata.]` These
summaries are descriptive, have no subgroup interval or p value, and cannot
rescue a registered contrast. State explicitly when an equal-model mean is
unavailable because any model stratum is empty.

## 7. Exploratory topology, balance and coordination descriptors

`[Insert all 13 frozen source-minus-registered-binary descriptors for four
source families, four sizes, three parent-model strata and each phase from the
strict descriptive projection.]` Report these records as exploratory
consistency evidence only. Use `was directionally consistent with` or `was not
directionally consistent with`; never use `mediated`, `caused`, `explained` or
`mechanism confirmed`. Formal and confirmation descriptor projections remain
separate and provide no interval or p value.

## 8. Runtime and external-validity boundary

`[Insert measured phase runtime, peak-memory evidence and completion witness.]`
The registered inference is bounded to the synthetic parent ensembles,
traffic kernels, capacities, horizon and router. `[Insert Lightning structural
diagnostic only if its exact snapshot has been lawfully and durably preserved;
report the source-rights and overlap boundaries alongside it.]` Any Lightning
result remains diagnostic-only and never enters the synthetic
parent-stratified bootstrap.

## Fixed display inventory

- Figure 1: result-free study and evidence flow.
- Figure 2: all eight global contrasts, both phases, adjusted bounds.
- Figure 3: complete `8 × 5` cross-phase replication-state matrix, with paired
  formal/confirmation interval-direction glyphs and gate border or hatching.
- Table 1: complete eight-row global numerical registry.
- Source Data: all 40 phase intervals, 40 replication states, parent values,
  event coverage, activity sensitivity and all 13 descriptive metrics.

## Assumptions or missing inputs

- Both phase summaries and phase evidence are incomplete or absent.
- Cross-phase replication evidence is absent.
- Both descriptive projections are absent.
- No effect direction, interval, gate state or replication state may be filled
  until the complete trust gate passes.
- The Results and main displays target 2.6 printed pages. The eight evidence
  jobs may be merged into four concise subsections, while complete registries
  remain in Source Data; any rendered overrun enters the frozen page-overrun
  decision gate rather than triggering selective omission.
