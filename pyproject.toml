[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "secondaryexploration"
version = "0.1.0"
description = "Reproducible experiments for service reliability in hypergraph payment networks"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["secondaryexploration"]
